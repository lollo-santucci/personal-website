---
# Service Template
# Copy this file, rename to your-slug.mdx, and fill in the fields.

title: "Service Title" # REQUIRED — string
slug: "service-slug" # REQUIRED — Slug (kebab-case, URL-safe)
description: "What this service is" # REQUIRED — string
relatedProjects: # OPTIONAL — Slug[] (project slugs, kebab-case)
  - "project-slug"
cta: # OPTIONAL — object (call to action)
  text: "Get in touch" # REQUIRED (if cta) — string
  href: "/contact" # REQUIRED (if cta) — string (URL or path)
order: 0 # OPTIONAL — number (display order)
---

{/* Body content goes here — maps to the "content" field in the Service interface */}
