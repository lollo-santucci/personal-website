---
# Project Template
# Copy this file, rename to your-slug.mdx, and fill in the fields.

title: "Project Title" # REQUIRED — string
slug: "project-slug" # REQUIRED — Slug (kebab-case, URL-safe)
description: "Short project pitch" # REQUIRED — string
stack: # REQUIRED — string[]
  - "TypeScript"
  - "Next.js"
categories: # REQUIRED — string[]
  - "web"
  - "ai"
status: "completed" # REQUIRED — "completed" | "in-progress" | "ongoing"
highlight: false # REQUIRED — boolean (true = featured project)
links: # OPTIONAL — object
  live: "https://example.com" # OPTIONAL — string (URL)
  github: "https://github.com/user/repo" # OPTIONAL — string (URL)
  demo: "https://demo.example.com" # OPTIONAL — string (URL)
image: "/assets/projects/project-slug.png" # OPTIONAL — AssetPath (file path to hero image)
order: 0 # OPTIONAL — number (display order)
---

{/* Body content goes here — maps to the "content" field in the Project interface */}
